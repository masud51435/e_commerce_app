//List of Enums
// enum cannot be created inside a class.

enum ProductType {single, variable}

enum TextSizes { small, medium, large }

enum OrderState { processing, shipped, delivered }

enum PaymentMethods {
  payPal,
  googlePay,
  bkash,
  nogod,
  visa,
  masterCard,
  creditCard,
  paytm,
  razorPay
}
